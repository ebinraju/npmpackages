import { Empty } from '../../components/empty/index.jsx';

export default {
  component: Empty,
  title: 'Empty',
  argTypes: {
  }
};

export const Default = {
  args: {
    title: 'No data found!'
  }
};
